# Slim dependency set for the cloud collector (GitHub Actions).
# Just enough to poll yfinance quotes and write parquet — keeps cold start fast
# and avoids pulling xgboost/lightgbm/optuna/nsepython on every 5-minute run.
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
pyyaml>=6.0
yfinance>=0.2.40
tenacity>=8.3
