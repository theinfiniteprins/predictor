"""Phase 3 — triple-barrier labeling (not yet implemented).

Planned modules:
    volatility.py     sigma_intraday = ATR(atr_window) / close, prior sessions only
    entry_points.py   rolling entry grid (delegates to predictor.calendar.entry_points)
    triple_barrier.py first-touch labeler; intrabar ties broken with 1-min bars
"""
