"""Phase 6 — backtesting (not yet implemented).

Planned module:
    engine.py  replay fired signals through the true triple-barrier exit logic with
               realistic slippage; no look-ahead (features use only info available
               at the entry timestamp); sweep k and the confidence threshold.
"""
